#given a list of integers; sum the even & odd integers.


def alternatingSums(a):

    first = 0
    second = 0

    for index, num in enumerate(a):
        if index % 2 == 1:
            first += num
        elif index % 2 == 0:
            second += num

    result = [second, first]
    return result
